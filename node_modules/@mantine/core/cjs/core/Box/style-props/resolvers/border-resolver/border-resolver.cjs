'use client';
'use strict';

var rem = require('../../../../utils/units-converters/rem.cjs');
require('react');
require('react/jsx-runtime');
require('@mantine/hooks');
var colorsTuple = require('../../../../MantineProvider/color-functions/colors-tuple/colors-tuple.cjs');
require('../../../../MantineProvider/Mantine.context.cjs');
require('../../../../MantineProvider/default-theme.cjs');
require('../../../../MantineProvider/MantineProvider.cjs');
require('../../../../MantineProvider/MantineThemeProvider/MantineThemeProvider.cjs');
require('../../../../MantineProvider/MantineCssVariables/MantineCssVariables.cjs');
var colorResolver = require('../color-resolver/color-resolver.cjs');

function borderResolver(value, theme) {
  if (typeof value === "number") {
    return rem.rem(value);
  }
  if (typeof value === "string") {
    const [size, style, ...colorTuple] = value.split(" ").filter((val) => val.trim() !== "");
    let result = `${rem.rem(size)}`;
    style && (result += ` ${style}`);
    colorsTuple.colorsTuple.length > 0 && (result += ` ${colorResolver.colorResolver(colorTuple.join(" "), theme)}`);
    return result.trim();
  }
  return value;
}

exports.borderResolver = borderResolver;
//# sourceMappingURL=border-resolver.cjs.map
